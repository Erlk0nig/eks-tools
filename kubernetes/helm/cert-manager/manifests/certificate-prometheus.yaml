apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-cert
  namespace: envoy-gateway-system
spec:
  secretName: prometheus-tls
  dnsNames:
  - aziz-soudani-prometheus.eks.karimarous.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod